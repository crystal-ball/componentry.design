// @ts-check

'use strict'

const { Alert } = require('./components/alert')
const { Button } = require('./components/button')
const { Text } = require('./components/text')

module.exports = { Alert, Button, Text }
