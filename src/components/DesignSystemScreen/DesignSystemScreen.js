import React from 'react'

import { ScreenContainer } from '@/components/universal'
import DesignSystem from './DesignSystem.mdx'

// 1. /design-system - overview of how styles and variants are created and customized
// 2. typography
// 2. spacing
// 3. colors

export default function DesignSystemScreen() {
  return (
    <ScreenContainer>
      <DesignSystem />
    </ScreenContainer>
  )
}
