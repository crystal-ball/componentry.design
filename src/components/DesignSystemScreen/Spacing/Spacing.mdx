<Text variant='heading-1' mt='xl'>
  Spacing
</Text>

The spacing values in the theme are used to create the spacing utility styles.

```scss
$theme: (
  spacing: (
    0: 0,
    xs: 0.25rem,
    sm: 0.5rem,
    md: 1rem,
    lg: 2rem,
    xl: 3rem,
  ),
);
```
