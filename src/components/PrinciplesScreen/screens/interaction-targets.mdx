# Library interaction targets

1. Any navigation target should be an `<a>` element and share a consistent API. A factory
   should be used to generate the variants.
2. All other interaction targets should be `<button>` elements and share a consistent API.
   A factory should be used to generate the variants.

## Creating variants for interaction targets

1. The default variant for interaction targets is to match link styles.
