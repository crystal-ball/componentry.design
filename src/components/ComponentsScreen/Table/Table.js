// 📝 Table notes:
// Virtualized table requires divs, and CSS grid works best with divs, so
// we're just diving in and using divs with aria roles to provide a11y.

// Create row spacing by setting grid columns style for 🅲-table-row
