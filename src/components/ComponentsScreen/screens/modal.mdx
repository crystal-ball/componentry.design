import { Text } from 'componentry'
import { ComponentHeading } from '@/components/universal'

<ComponentHeading
  name='Modal'
  components={[
    'Modal',
    'Modal.Body',
    'Modal.Close',
    'Modal.Footer',
    'Modal.Header',
    'Modal.Title',
  ]}
/>
<Text variant='lead' />

```javascript

```
