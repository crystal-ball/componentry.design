import { Text } from 'componentry'
import { ComponentHeading } from '@/components/universal'

<ComponentHeading
  name='Popover'
  components={[
    'Popover',
    'Popover.Body',
    'Popover.Content',
    'Popover.Heading',
    'Popover.Title',
  ]}
/>
<Text variant='lead' />

```javascript

```
