<ComponentHeading name='List' components={['List', 'List.Item']} />
<Text variant='lead'></Text>

```javascript
```

- Lists use a `ul` container element by default, but only `li` elements are valid children
  of an unordered list, so you should override it to a `div` if there are none list item
  children.
- `List.Item` components will render an `a` if passed an href, a `button` if passed on
  onClick, or a `li`.
