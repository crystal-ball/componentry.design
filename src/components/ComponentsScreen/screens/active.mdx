import { Text } from 'componentry'
import { ComponentHeading } from '@/components/universal'

<ComponentHeading
  name='Active'
  components={['Active', 'Active.Content', 'Active.Trigger']}
/>
<Text variant='lead'>Manage active state for an element.</Text>

```javascript
<Active>
  <Active.Trigger>Active Trigger</Active.Trigger>
  <Active.Content>
    Content display toggled by the <code>Trigger</code> component.
  </Active.Content>
</Active>
```

## Terms

- Binary-active vs Compound-active: Used to describe whether an active context has twp or
  more than two active states. eg binary active is an active/!active tooltip, whereas tabs
  are a compound active with activeIds
